name: Snowflake SQL Deployment

on:
  push:
    branches: [master]
    paths:
      - "sql/**"
  pull_request:
    branches: [master]
    paths:
      - "sql/**"
  workflow_dispatch: # Allow manual trigger

env:
  SNOWSQL_VERSION: "1.2.24"

jobs:
  validate:
    name: Validate SQL Scripts
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Install SQLFluff
        run: pip install sqlfluff

      - name: Lint SQL files
        run: |
          echo "Running SQL validation..."
          # Only lint the actual SQL files, skip the setup file
          for sql_file in sql/[1-9]*.sql; do
            if [ -f "$sql_file" ]; then
              echo "Validating $sql_file"
              sqlfluff lint "$sql_file" --dialect snowflake
            fi
          done

  deploy-dev:
    name: Deploy to DEV
    needs: validate
    runs-on: ubuntu-latest
    environment: dev

    steps:
      - uses: actions/checkout@v3

      - name: Install and Configure SnowSQL
        run: |
          # Download and install SnowSQL
          curl -O https://sfc-repo.snowflakecomputing.com/snowsql/bootstrap/1.2/linux_x86_64/snowsql-${{ env.SNOWSQL_VERSION }}-linux_x86_64.bash
          mkdir -p ~/snowflake
          SNOWSQL_DEST=~/snowflake SNOWSQL_LOGIN_SHELL=~/.profile bash snowsql-${{ env.SNOWSQL_VERSION }}-linux_x86_64.bash
          
          # Create SnowSQL config directory
          mkdir -p ~/.snowsql
          
          # Create SnowSQL config file with proper connection details
          cat > ~/.snowsql/config << EOF
          [connections.dev]
          accountname = ${{ secrets.SNOWFLAKE_ACCOUNT }}
          username = ${{ secrets.SNOWFLAKE_USERNAME }}
          password = ${{ secrets.SNOWFLAKE_PASSWORD }}
          rolename = DEV_ROLE
          warehousename = DEV_WH
          dbname = DEV_DB

          [connections.qa]
          accountname = ${{ secrets.SNOWFLAKE_ACCOUNT }}
          username = ${{ secrets.SNOWFLAKE_USERNAME }}
          password = ${{ secrets.SNOWFLAKE_PASSWORD }}
          rolename = QA_ROLE
          warehousename = QA_WH
          dbname = QA_DB

          [connections.prod]
          accountname = ${{ secrets.SNOWFLAKE_ACCOUNT }}
          username = ${{ secrets.SNOWFLAKE_USERNAME }}
          password = ${{ secrets.SNOWFLAKE_PASSWORD }}
          rolename = PROD_ROLE
          warehousename = PROD_WH
          dbname = PROD_DB
          EOF
          
          # Set proper permissions
          chmod 600 ~/.snowsql/config

      - name: Deploy to DEV
        env:
          SNOWFLAKE_ACCOUNT: ${{ secrets.SNOWFLAKE_ACCOUNT }}
          SNOWFLAKE_USERNAME: ${{ secrets.SNOWFLAKE_USERNAME }}
          SNOWFLAKE_PASSWORD: ${{ secrets.SNOWFLAKE_PASSWORD }}
          SNOWFLAKE_ROLE: DEV_ROLE
          SNOWFLAKE_WAREHOUSE: DEV_WH
          SNOWFLAKE_DATABASE: DEV_DB
        run: |
          echo "Running SQL scripts in DEV environment"
          # First run the setup script
          ~/snowflake/snowsql -c dev -f sql/00_setup.sql \
                             --variable env_role="$SNOWFLAKE_ROLE" \
                             --variable env_db="$SNOWFLAKE_DATABASE" \
                             --variable env_wh="$SNOWFLAKE_WAREHOUSE" \
                             --variable env_schema="RAW"
          
          # Then run all other SQL files
          for sql_file in sql/[1-9]*.sql; do
            if [ -f "$sql_file" ]; then
              echo "Executing $sql_file"
              ~/snowflake/snowsql -c dev -f "$sql_file"
            fi
          done

  deploy-qa:
    name: Deploy to QA
    needs: deploy-dev
    runs-on: ubuntu-latest
    environment:
      name: qa
      url: ${{ steps.deploy.outputs.deployment-url }}

    steps:
      - uses: actions/checkout@v3

      - name: Install SnowSQL
        run: |
          curl -O https://sfc-repo.snowflakecomputing.com/snowsql/bootstrap/1.2/linux_x86_64/snowsql-${{ env.SNOWSQL_VERSION }}-linux_x86_64.bash
          SNOWSQL_DEST=~/snowflake SNOWSQL_LOGIN_SHELL=~/.profile bash snowsql-${{ env.SNOWSQL_VERSION }}-linux_x86_64.bash

      - name: Deploy to QA
        id: deploy
        env:
          SNOWFLAKE_ACCOUNT: ${{ secrets.SNOWFLAKE_ACCOUNT }}
          SNOWFLAKE_USERNAME: ${{ secrets.SNOWFLAKE_USERNAME }}
          SNOWFLAKE_PASSWORD: ${{ secrets.SNOWFLAKE_PASSWORD }}
          SNOWFLAKE_ROLE: QA_ROLE
          SNOWFLAKE_WAREHOUSE: QA_WH
          SNOWFLAKE_DATABASE: QA_DB
        run: |
          echo "Running SQL scripts in QA environment"
          for sql_file in sql/*.sql; do
            ~/snowflake/snowsql -a "$SNOWFLAKE_ACCOUNT" \
                               -u "$SNOWFLAKE_USERNAME" \
                               -p "$SNOWFLAKE_PASSWORD" \
                               -r "$SNOWFLAKE_ROLE" \
                               -w "$SNOWFLAKE_WAREHOUSE" \
                               -d "$SNOWFLAKE_DATABASE" \
                               -f "$sql_file"
          done

  deploy-prod:
    name: Deploy to Production
    needs: deploy-qa
    runs-on: ubuntu-latest
    environment:
      name: prod
      url: ${{ steps.deploy.outputs.deployment-url }}

    steps:
      - uses: actions/checkout@v3

      - name: Install SnowSQL
        run: |
          curl -O https://sfc-repo.snowflakecomputing.com/snowsql/bootstrap/1.2/linux_x86_64/snowsql-${{ env.SNOWSQL_VERSION }}-linux_x86_64.bash
          SNOWSQL_DEST=~/snowflake SNOWSQL_LOGIN_SHELL=~/.profile bash snowsql-${{ env.SNOWSQL_VERSION }}-linux_x86_64.bash

      - name: Deploy to PROD
        id: deploy
        env:
          SNOWFLAKE_ACCOUNT: ${{ secrets.SNOWFLAKE_ACCOUNT }}
          SNOWFLAKE_USERNAME: ${{ secrets.SNOWFLAKE_USERNAME }}
          SNOWFLAKE_PASSWORD: ${{ secrets.SNOWFLAKE_PASSWORD }}
          SNOWFLAKE_ROLE: PROD_ROLE
          SNOWFLAKE_WAREHOUSE: PROD_WH
          SNOWFLAKE_DATABASE: PROD_DB
        run: |
          echo "Running SQL scripts in PROD environment"
          for sql_file in sql/*.sql; do
            ~/snowflake/snowsql -a "$SNOWFLAKE_ACCOUNT" \
                               -u "$SNOWFLAKE_USERNAME" \
                               -p "$SNOWFLAKE_PASSWORD" \
                               -r "$SNOWFLAKE_ROLE" \
                               -w "$SNOWFLAKE_WAREHOUSE" \
                               -d "$SNOWFLAKE_DATABASE" \
                               -f "$sql_file"
          done
